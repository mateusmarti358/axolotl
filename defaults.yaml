Shift:
  # applies to 2 components at once (R, G, B)
  channels: 'RB'

DiagonalMatrix:
  square_size: 2
  thickness: 1
  diagonals: True

Filter:
  threshold: 0.0

Crt:
  scanline_width: 1.0
  scanline_intensity: 1.0
  warp: 1.0
  vignette: 1.0
  vertical_components: True

BoxBloom:
  distance: 7
  loss: 0.99

GaussBloom:
  radius: 24
  sigma: 6.0
  threshold: 0.5
  mix: 1.0
